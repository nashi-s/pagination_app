<h1>投稿一覧</h1>
 
<%= form_with model: @post, local: true do |f| %>
  <%= f.text_field :body %>
  <%= f.submit "送信" %>
<% end %>
<article>
  <% @posts.each do |post| %>
    <section>
    <p><%= post.body %></p>
    </section>
  <% end %>
</article>
<%= paginate @posts %>

